<?xml version="1.0" encoding="UTF-8"?>
<svg width="100%" height="200" viewBox="0 0 800 200" xmlns="http://www.w3.org/2000/svg" version="1.1">
  <style>
    .background {
      fill: url(#bgGradient);
    }

    .symbol {
      font: bold 24px monospace;
      opacity: 0.9;
      animation: float 6s ease-in-out infinite;
    }

    .symbol2 {
      font: bold 20px monospace;
      opacity: 0.6;
      animation: float2 8s ease-in-out infinite;
    }

    .creature {
      animation: flap 4s ease-in-out infinite, drift 10s linear infinite;
      transform-origin: center;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-15px); }
    }

    @keyframes float2 {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }

    @keyframes flap {
      0%, 100% { transform: scale(1) rotate(0); }
      50% { transform: scale(1.05) rotate(2deg); }
    }

    @keyframes drift {
      0% { transform: translateX(0); }
      100% { transform: translateX(800px); }
    }
  </style>

  <defs>
    <linearGradient id="bgGradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#1a002d; stop-opacity:1" />
      <stop offset="100%" style="stop-color:#3d004d; stop-opacity:1" />
    </linearGradient>
  </defs>

  <rect width="100%" height="200" class="background" />

  <!-- Symbols floating -->
  <text x="50" y="100" fill="#ff99ff" class="symbol">{{}}</text>
  <text x="150" y="130" fill="#cc66ff" class="symbol2">&lt;/&gt;</text>
  <text x="250" y="80" fill="#ff66cc" class="symbol">[]</text>
  <text x="350" y="120" fill="#e066ff" class="symbol2">&lt;>&lt;/&gt;</text>
  <text x="450" y="90" fill="#ff33cc" class="symbol">()</text>

  <!-- Lovecraftian creature silhouette -->
  <g class="creature">
    <circle cx="100" cy="50" r="10" fill="#ff33cc" />
    <path d="M100 50 Q90 60 95 70 Q100 60 105 70 Q110 60 100 50" fill="#ff99ff"/>
    <path d="M90 45 Q80 55 85 65" stroke="#ff66cc" stroke-width="2" fill="none"/>
    <path d="M110 45 Q120 55 115 65" stroke="#ff66cc" stroke-width="2" fill="none"/>
    <path d="M95 35 Q100 20 105 35" stroke="#cc33ff" stroke-width="3" fill="none"/>
  </g>
</svg>
